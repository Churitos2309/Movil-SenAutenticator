import 'package:flutter/material.dart';

class HistorialPageInstructor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Historial Page'),
    );
  }
}